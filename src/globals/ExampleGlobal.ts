import { GlobalConfig } from 'payload/types';

const ExampleGlobal: GlobalConfig = {
  slug: 'example-global',
  fields: [
    {
      name: 'siteTitle',
      type: 'text',
    },
  ],
};

export default ExampleGlobal;
