{
  "name": "thav-payload-starter",
  "version": "1.0.0",
  "description": "Starter template for Payload CMS projects.",
  "scripts": {
    "dev": "payload dev",
    "build": "payload build",
    "start": "payload start"
  },
  "dependencies": {
    "payload": "^2.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
